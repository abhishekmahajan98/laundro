import 'package:flutter/material.dart';

class OrderConfirmPage extends StatelessWidget {
  // final washId, dryCleanId, ironId;
  // OrderConfirmPage({this.washId, this.dryCleanId, this.ironId});
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("ORDER CONFIRM")));
  }
}
